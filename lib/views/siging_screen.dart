import 'package:flutter/material.dart';

class SigingScreen extends StatelessWidget {
  const SigingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('signin screen')));
  }
}
